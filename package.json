{
  "name": "unity-ai",
  "version": "1.0.0",
  "description": "Unity AI is Enverus' internal AI agentic solution",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["ai", "agents", "enverus"],
  "author": "Enverus",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
